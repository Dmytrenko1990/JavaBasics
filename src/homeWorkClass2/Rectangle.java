package homeWorkClass2;

public class Rectangle {
    public static void main(String[] args) {
        int width = 5;
        int height = 8;
        double perimeter = 2 * (width + height);
        double area = width * height;
        System.out.println("The perimeter of a rectangle with width " + width + " and height " + height + " is equal to " + perimeter + " and the area " + area);

    }
}
